import {StyleSheet} from 'react-native';

const images = {

};

const styles = StyleSheet.create({
  newsItem: {
    marginBottom: 7,
    width: '100%',
    borderWidth: 0.5,
    borderColor: '#000000'
  }
});

export {images, styles};
