import {StyleSheet} from 'react-native';
import * as globals from '../../lib/globals'

const images = {

};

const styles = StyleSheet.create({
  thumbnail: {
    marginBottom: 5,
  },
  content: {
    paddingHorizontal: 5
  },
  description: {
    color: globals.COLORS.TEXT_COLOR
  }
});

export {images, styles};
